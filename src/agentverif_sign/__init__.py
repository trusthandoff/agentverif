"""agentverif-sign — sign and verify AI agent packages."""

__version__ = "0.1.0"

# Optional LangChain integration — available only when langchain-core is installed.
# Import explicitly when needed:
#   from agentverif_sign.langchain_tool import verify_tool
